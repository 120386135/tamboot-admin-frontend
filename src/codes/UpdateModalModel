import { effectsWithCallback } from '@/utils/ModelTemplate';
import { update } from '@/services/systemRole';

export default {
    namespace: 'systemRole',

    state: {
    },

    effects: {
        ...effectsWithCallback(update, 'update'),
    },

    reducers: {
    }
}